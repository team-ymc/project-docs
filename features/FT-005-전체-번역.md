# Feature: FT-005 전체 번역

## 1. Overview

- Summary: 학습 페이지에서 논문 본문 전체를 원문 옆 또는 아래에 한국어 번역과 함께 볼 수 있다.
- Status: In Progress
- Tracking: YMC-383
- Depends on: FT-004 논문 학습 뷰어

## 2. Feature Boundary

### In Scope

- 전체 번역 표시 배치 순환(안 보기 → 아래 → 옆)과 배치 기억
- 원문과 번역을 함께 표시하는 렌더. 구조 단위를 유지하고 원문은 그대로 남는다
- 번역이 없는 논문에서의 컨트롤 비활성과 사유 안내
- 파싱 시점에 생성된 번역의 적재와 본문 조회 응답 제공

### Out of Scope

- 번역 언어 선택(한국어 외) → 후속(post-MVP)
- 사용자 요청 시점의 번역 생성·재생성 → 후속(post-MVP)
- 번역이 없는 기존 논문의 재파싱·재적재 → 후속(post-MVP)
- 번역 로딩·실패 처리 → 파싱 단계 책임(FT-003). 번역이 없는 패키지는 "번역 없음"으로 서빙한다
- 인라인(부분) 번역 → FT-006

## 3. Userflow References

| Userflow | Relation |
|---|---|
| UF-004 논문 전체 번역 보기 | 이 Feature가 직접 구현하는 flow |

## 4. Stories

### Story 1. 사용자는 논문 원문 옆이나 아래에서 한국어 번역을 함께 볼 수 있다 `MVP`

- type: USER
- Source Userflows: UF-004 Step 1-4
- Acceptance Criteria:
  - 툴바의 `번역` 버튼은 누를 때마다 안 보기 → 아래 → 옆 → 안 보기로 순환하고, 현재 배치를 라벨(`번역` / `번역 · 아래` / `번역 · 옆`)로 드러낸다.
  - `옆`은 원문과 번역을 두 열로, `아래`는 원문 바로 아래에 번역을 둔다. 두 배치 모두 원문을 번역으로 갈아끼우지 않는다.
  - 제목 계열·이미지·표·수식·참고문헌은 번역을 붙이지 않고 원문만 전폭으로 둔다.
  - 그림·표 캡션은 본문 문단과 같은 방식으로 번역을 붙인다.
  - 번역 텍스트는 선택할 수 없다. 선택 팝업과 인라인 액션은 원문에서만 열린다.
  - 채팅이 열려 있어도 `옆` 배치를 유지한다. `옆`으로 들어갈 때 채팅 기본 폭을 320px로 맞추고, 이후 폭은 사용자 조작을 따른다.
  - 번역이 없는 논문에서는 `번역` 버튼이 비활성이고, 한국어 논문인지 번역이 준비되지 않은 논문인지를 툴팁으로 구분해 안내한다.
  - 선택한 배치는 브라우저에 저장돼 다음 방문에 복원된다. 번역이 없는 논문에서는 저장값과 무관하게 안 보기로 적용하고 저장값은 지우지 않는다.
  - 화면 기준은 `design/v2/Paper Study Page.dc.html`의 트윅 `translationMode`(off/below/side) · `sourceLanguage`(en/ko/und) · `chat`(open/collapsed)이다.
- Depends on: FT-004 Story 2

### Story 2. 시스템은 파싱 시점에 만들어 둔 한국어 번역을 학습 화면에 제공한다 `MVP`

- type: SYSTEM
- Source Userflows: UF-004 Step 2-3
- Acceptance Criteria:
  - 번역은 논문 파싱 시점에 문서 패키지 안에 함께 생성된다. 사용자가 전체 번역을 켤 때 새로 생성하지 않는다.
  - 시스템은 패키지의 블록별 한국어 번역과 문서 언어를 적재하고, 본문 조회 응답에 원문과 함께 담아 내려준다.
  - 번역 존재는 블록의 번역 필드 유무로 판단한다. 문서 언어가 영어여도 번역이 없는 논문이 있다.
  - 번역 필드가 없거나 형식이 어긋난 패키지도 경고만 남기고 적재를 계속하며, 해당 논문은 번역 없는 상태로 서빙한다.
  - 이미 적재된 논문은 다시 파싱하지 않는다. 번역이 없는 기존 논문은 번역 없는 상태로 남는다.
- Depends on: FT-003 Story 4

## 5. Done Criteria

- 사용자는 원문을 그대로 둔 채 옆·아래 배치로 번역을 함께 읽고 안 보기로 돌아갈 수 있다.
- 번역이 없는 논문은 비활성 컨트롤과 안내로 구분된다.

## 6. Open Questions

- ~~번역 생성 시점: 분석 단계에서 미리 생성 vs 요청 시 on-demand~~

> **Resolved** — 파싱 시점에 AI 파서가 문서 패키지 안에 미리 생성한다(YMC-360). BE는 적재·서빙, FE는 표시만 한다.

- ~~캐싱 정책과 번역 단위(문단 단위 점진 표시/스트리밍 여부)~~

> **Resolved** — 패키지에 저장된 결과를 그대로 서빙하므로 별도 캐시가 없다. 표시 단위는 블록이고 점진 표시·스트리밍은 두지 않는다.

- ~~번역 생성은 외부 AI 서버 호출 — 생성 시점 결정 후 BE↔AI 서버 seam을 contracts로 박는다.~~

> **Resolved** — BE가 읽는 패키지 필드는 ai 저장소 `README.md`(Configure Document Parser)와 `docs/S3_BUCKET_STRUCTURE_KO.md`가 정의하고, FE에 내려주는 형태는 `contracts/frontend-backend/openapi.yaml`의 `PaperContentResponse.sourceLanguage`와 `PaperTextContent.textKor`다.
